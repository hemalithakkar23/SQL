EDLCR 
REM 1 DEPARTMENT_NAME , STATE_PROVINCE  
SELECT DEPARTMENT_NAME , STATE_PROVINCE  
FROM DEPARTMENTS NATURAL JOIN LOCATIONS;
SELECT DEPARTMENT_NAME , STATE_PROVINCE  
FROM DEPARTMENTS  JOIN LOCATIONS USING (LOCATION_ID);


2 CITY , COUNTRY_NAME
SELECT CITY , COUNTRY_NAME FROM LOCATIONS NATURAL JOIN COUNTRIES;
SELECT CITY , COUNTRY_NAME FROM LOCATIONS JOIN COUNTRIES USING(COUNTRY_ID);

REM 3 STATE_PROVINCE , COUNTRY_NAME , REGION_NAME    
SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME 
FROM LOCATIONS NATURAL JOIN COUNTRIES
NATURAL JOIN REGIONS  ;

SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME 
FROM LOCATIONS  JOIN COUNTRIES USING (COUNTRY_ID)
JOIN REGIONS USING (REGION_ID)  ;



 4 REGION_NAME , COUNTRY_NAME , DEPARTMENT_NAME

SELECT REGION_NAME , COUNTRY_NAME , DEPARTMENT_NAME
FROM DEPARTMENTS NATURAL JOIN LOCATIONS NATURAL JOIN COUNTRIES
NATURAL JOIN REGIONS ; 

SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME 
FROM DEPARTMENTS JOIN LOCATIONS USING(LOCATION_ID)
 JOIN COUNTRIES USING (COUNTRY_ID)
JOIN REGIONS USING (REGION_ID)  ;



SELECT E.FIRST_NAME , D.DEPARTMENT_NAME , DEPARTMENT_ID,
E.MANAGER_ID , D.MANAGER_ID  
FROM EMPLOYEES E JOIN DEPARTMENTS D USING (DEPARTMENT_ID);

SELECT E.FIRST_NAME , D.DEPARTMENT_NAME , D.DEPARTMENT_ID,
E.MANAGER_ID , D.MANAGER_ID  
FROM EMPLOYEES E JOIN DEPARTMENTS D ON (D.DEPARTMENT_ID=E.DEPARTMENT_ID);

EDLCR 
 NEW
REM 1 DEPARTMENT_NAME , CITY
SELECT  D.DEPARTMENT_NAME , L.CITY 
FROM DEPARTMENTS D JOIN LOCATIONS L USING (LOCATION_ID);

SELECT  D.DEPARTMENT_NAME , L.CITY 
FROM DEPARTMENTS D JOIN LOCATIONS L ON (L.LOCATION_ID=D.LOCATION_ID);

SELECT  D.DEPARTMENT_NAME , L.CITY 
FROM DEPARTMENTS D JOIN LOCATIONS L ON (L.LOCATION_ID=D.LOCATION_ID);

SELECT  D.DEPARTMENT_NAME , L.CITY 
FROM DEPARTMENTS D NATURAL  JOIN LOCATIONS L;

2 STATE_PROVINCE , REGION_NAME

SELECT L.STATE_PROVINCE , R.REGION_NAME 
FROM LOCATIONS L JOIN COUNTRIES C USING (COUNTRY_ID)
JOIN REGIONS R USING (REGION_ID);

SELECT L.STATE_PROVINCE , R.REGION_NAME 
FROM LOCATIONS L JOIN COUNTRIES C ON (C.COUNTRY_ID=L.COUNTRY_ID)
JOIN REGIONS R ON (R.REGION_ID=C.REGION_ID);

SELECT L.STATE_PROVINCE , R.REGION_NAME 
FROM LOCATIONS L NATURAL JOIN COUNTRIES C 
NATURAL JOIN REGIONS R ;

EDLCR
3 REGION_NAME , COUNTRY_ID , CITY , DEPARTMENT_ID

SELECT R.REGION_NAME , COUNTRY_ID , CITY , D.DEPARTMENT_ID 
FROM DEPARTMENTS D JOIN LOCATIONS L USING(LOCATION_ID)
JOIN COUNTRIES C USING (COUNTRY_ID)
JOIN REGIONS R USING(REGION_ID);

SELECT R.REGION_NAME , C.COUNTRY_ID , CITY , D.DEPARTMENT_ID 
FROM DEPARTMENTS D JOIN LOCATIONS L ON (L.LOCATION_ID=D.LOCATION_ID)
JOIN COUNTRIES C ON (C.COUNTRY_ID=L.COUNTRY_ID)
JOIN REGIONS R ON(R.REGION_ID=C.REGION_ID);

SELECT R.REGION_NAME , COUNTRY_ID , CITY , D.DEPARTMENT_ID 
FROM DEPARTMENTS D NATURAL JOIN LOCATIONS L
NATURAL JOIN COUNTRIES C 
NATURAL JOIN REGIONS R ;

REM 1 NATURAL JOIN 2 INNER JOIN 'USING' 3 INNER JOIN 'ON'


2.NJ + ON 
SELECT FIRST_NAME , DEPARTMENT_NAME , CITY , COUNTRY_NAME , REGION_NAME , JOB_ID, JOB_TITLE ,SALARY,GRADE_LEVEL
FROM EMPLOYEES  JOIN DEPARTMENTS USING (DEPARTMENT_ID)
 JOIN LOCATIONS USING (LOCATION_ID) 
 JOIN COUNTRIES USING (COUNTRY_ID)
 JOIN REGIONS USING (REGION_ID)
 NATURAL JOIN JOBS
JOIN JOB_GRADES ON SALARY BETWEEN LOWEST_SAL AND HIGHEST_SAL;

3.USING + ON 
SELECT FIRST_NAME , DEPARTMENT_NAME , CITY , COUNTRY_NAME , REGION_NAME , JOB_ID, JOB_TITLE ,SALARY,GRADE_LEVEL
FROM EMPLOYEES  JOIN DEPARTMENTS USING (DEPARTMENT_ID)
 JOIN LOCATIONS USING (LOCATION_ID) 
 JOIN COUNTRIES USING (COUNTRY_ID)
 JOIN REGIONS USING (REGION_ID)
 JOIN JOBS USING (JOB_ID) 
JOIN JOB_GRADES ON SALARY BETWEEN LOWEST_SAL AND HIGHEST_SAL;

1. NJ + USING + ON 
SELECT FIRST_NAME , DEPARTMENT_NAME , CITY , COUNTRY_NAME , REGION_NAME , JOB_ID, JOB_TITLE ,SALARY,GRADE_LEVEL
FROM EMPLOYEES  JOIN DEPARTMENTS USING (DEPARTMENT_ID)
 JOIN LOCATIONS USING (LOCATION_ID) 
 JOIN COUNTRIES USING (COUNTRY_ID)
 JOIN REGIONS USING (REGION_ID)
 NATURAL JOIN JOBS
JOIN JOB_GRADES ON SALARY BETWEEN LOWEST_SAL AND HIGHEST_SAL;


SELECT FIRST_NAME , DEPARTMENT_NAME , CITY , COUNTRY_NAME , REGION_NAME , JOB_ID, JOB_TITLE ,SALARY,GRADE_LEVEL
FROM EMPLOYEES  JOIN DEPARTMENTS USING (DEPARTMENT_ID)
 JOIN LOCATIONS USING (LOCATION_ID)
 NATURAL JOIN COUNTRIES
 JOIN REGIONS USING (REGION_ID)
 JOIN JOBS USING (JOB_ID)
JOIN JOB_GRADES ON SALARY BETWEEN LOWEST_SAL AND HIGHEST_SAL;




SELECT DEPARTMENT_NAME , REGION_NAME FROM 
DEPARTMENTS JOIN LOCATIONS USING (LOCATION_ID) 
JOIN COUNTRIES USING (COUNTRY_ID)
JOIN REGIONS USING (REGION_ID);


SELECT DEPARTMENT_NAME , REGION_NAME FROM 
REGIONS JOIN COUNTRIES USING (REGION_ID)
JOIN LOCATIONS USING (COUNTRY_ID)
JOIN DEPARTMENTS USING (LOCATION_ID);

SELECT D.DEPARTMENT_NAME , REGION_NAME FROM 
 LOCATIONS L  JOIN  DEPARTMENTS D USING  (LOCATION_ID) 
JOIN COUNTRIES C ON  (C.COUNTRY_ID=L.COUNTRY_ID)
JOIN REGIONS USING (REGION_ID);


SELECT D.DEPARTMENT_NAME , REGION_NAME FROM 
COUNTRIES C JOIN 
LOCATIONS L ON  (C.COUNTRY_ID=L.COUNTRY_ID)
JOIN REGIONS USING (REGION_ID)
JOIN DEPARTMENTS D USING  (LOCATION_ID) ;





