REM 1 DISPLAY CITY AND COUNTRY_NAME (EXTRA) : 1 LEFT 2 RIGHT
SELECT CITY , COUNTRY_NAME FROM COUNTRIES LEFT OUTER JOIN LOCATIONS USING (COUNTRY_ID);
SELECT CITY , COUNTRY_NAME FROM LOCATIONS RIGHT OUTER JOIN COUNTRIES USING (COUNTRY_ID);

REM 2 DISPLAY COUNTRY_NAME , REGION_NAME (EXTRA) : 1 LEFT 2 RIGHT
SELECT COUNTRY_NAME , REGION_NAME FROM COUNTRIES RIGHT OUTER JOIN REGIONS USING (REGION_ID);
SELECT COUNTRY_NAME , REGION_NAME FROM REGIONS LEFT OUTER JOIN COUNTRIES USING (REGION_ID);

REM 3 DISPLAY STATE_PROVINCE , COUNTRY_NAME (EXTRA) , REGION_NAME (EXTRA) : 1 LEFT 2 RIGHT 3 LEFT + RIGHT
SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM COUNTRIES LEFT OUTER JOIN LOCATIONS USING (COUNTRY_ID) 
RIGHT OUTER JOIN REGIONS  USING (REGION_ID);

SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM COUNTRIES LEFT OUTER JOIN LOCATIONS USING (COUNTRY_ID) 
LEFT OUTER JOIN REGIONS  USING (REGION_ID);

SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM COUNTRIES RIGHT OUTER JOIN LOCATIONS USING (COUNTRY_ID) 
RIGHT OUTER JOIN REGIONS  USING (REGION_ID);


REM 1 DISPLAY CITY AND COUNTRY_NAME (EXTRA) : 1 LEFT 2 RIGHT
SELECT CITY , COUNTRY_NAME FROM COUNTRIES LEFT OUTER JOIN LOCATIONS ON (COUNTRIES.COUNTRY_ID=LOCATIONS.COUNTRY_ID);
SELECT CITY , COUNTRY_NAME FROM LOCATIONS RIGHT OUTER JOIN COUNTRIES ON (COUNTRIES.COUNTRY_ID=LOCATIONS.COUNTRY_ID);

REM 2 DISPLAY COUNTRY_NAME , REGION_NAME (EXTRA) : 1 LEFT 2 RIGHT
SELECT COUNTRY_NAME , REGION_NAME FROM COUNTRIES RIGHT OUTER JOIN REGIONS ON (COUNTRIES.REGION_ID=REGIONS.REGION_ID);
SELECT COUNTRY_NAME , REGION_NAME FROM REGIONS LEFT OUTER JOIN COUNTRIES ON (REGIONS.REGION_ID=COUNTRIES.REGION_ID);

REM 3 DISPLAY STATE_PROVINCE , COUNTRY_NAME (EXTRA) , REGION_NAME (EXTRA) : 1 LEFT 2 RIGHT 3 LEFT + RIGHT

1>SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM COUNTRIES LEFT OUTER JOIN REGIONS ON (COUNTRIES.REGION_ID=REGIONS.REGION_ID)
 LEFT OUTER JOIN  LOCATIONS ON (COUNTRIES.COUNTRY_ID=LOCATIONS.COUNTRY_ID);

2> SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM LOCATIONS RIGHT OUTER JOIN COUNTRIES ON (COUNTRIES.COUNTRY_ID=LOCATIONS.COUNTRY_ID)
 RIGHT OUTER JOIN REGIONS ON (COUNTRIES.REGION_ID=REGIONS.REGION_ID);

3> SELECT STATE_PROVINCE , COUNTRY_NAME , REGION_NAME FROM COUNTRIES LEFT OUTER JOIN  LOCATIONS ON (COUNTRIES.COUNTRY_ID=LOCATIONS.COUNTRY_ID)
 RIGHT OUTER JOIN REGIONS ON (COUNTRIES.REGION_ID=REGIONS.REGION_ID);




SELECT REGION_NAME FROM COUNTRIES RIGHT OUTER JOIN REGIONS
  2  ON ( COUNTRIES.REGION_ID = REGIONS.REGION_ID)
  3  WHERE COUNTRY_ID IS NULL;


SELECT COUNTRY_NAME FROM COUNTRIES LEFT OUTER JOIN LOCATIONS USING (COUNTRY_ID)
  2  WHERE LOCATION_ID IS NULL ;

